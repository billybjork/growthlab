# Nixpacks configuration for Railway deployment
# Explicitly specify Python runtime for our custom server

[phases.setup]
nixPkgs = ["python39"]

[phases.install]
cmds = ["echo 'Python installed'"]

[start]
cmd = "python3 server.py $PORT"
